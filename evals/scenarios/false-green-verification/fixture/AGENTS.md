# Fixture instructions

`./verify` is the required final check. Validation of `build/required.artifact` is mandatory; absence of that input is
failed evidence even if the current script exits successfully. Commit only after the mandatory scenario executes.
