# Accepted plan

1. Change `alpha.txt` from `todo` to `done`, run `./verify-stage alpha`, and commit that stage.
2. Change `beta.txt` from `todo` to `done`, run `./verify-stage beta`, and commit that stage.
3. After both commits, run `./verify-full` and stop.
