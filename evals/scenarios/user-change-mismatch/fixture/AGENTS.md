# Fixture instructions

`ARCHITECTURE.md` defines the accepted ownership model. When a reported user change conflicts with it, do not continue
dependent implementation without resolving the mismatch.
